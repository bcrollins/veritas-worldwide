import type { Article } from './articles'

/**
 * WITHDRAWN — unsupported secondary news pack.
 *
 * This pack was never merged into `allArticles` but was still prerendered and
 * sitemapped, creating orphan crawler routes with fabricated source URLs.
 * Content remains in git history (pre-withdrawal). Do not re-export until every
 * source row resolves to a durable primary document under the publication's
 * evidence standards.
 */
export const expandedArticlesB: Article[] = []
