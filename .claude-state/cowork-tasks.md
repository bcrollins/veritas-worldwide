# Veritas Audit Queue — 2026-04-22

- [x] P1 — Expanded the manuscript/course surface in a first April 22 increment: added Chapter 29, registered the evidence-methods topic hub, deepened Institute course/manual summaries, corrected route counts, fixed the HubSpot script URL, added a global mobile overflow guard, and pushed `f9bb7a6`. Production advanced to `f9bb7a6` during the run.
- [x] P1 — Rebuilt the Israel dossier local experience around source-first public records: added a searchable Source Workbench, deduplicated incident filtering, refreshed CRS/OCHA/UNICEF/CPJ figures, tightened attribution language across page/PDF/carousel/chapter-15, and replaced brittle Wikimedia thumbnail dependencies with local OG assets.
- [x] P1 — Verified the Israel dossier enhancement locally with `npx tsc --noEmit`, `npm run build`, `npm run verify:bundle`, `npm run verify:sources`, Twitterbot/crawler metadata checks, six-viewport Playwright screenshots, source/incident interaction tests, slow-3G, and blocked-image resilience checks.
- [x] P1 — Live-verified `/israel-dossier` after production advanced beyond `f9bb7a6`: commit `02ecbf0` showed the source workbench, refreshed CRS/OCHA/UNICEF/CPJ figures, local imagery, canonical/OG metadata, and no horizontal overflow across the primary viewport matrix.
- [x] P2 — Consolidated the Israel dossier public-record canon into `src/data/israelDossierCanon.ts`, wired the page/PDF/carousel/chapter companions to it, added `npm run verify:israel-dossier`, fixed prerender route discovery for canonical Chapter 15 metadata, restored the sitemap to 368 URLs after a caught local regression, and live-verified production commit `029f323` on April 22, 2026.
- [x] P2 — Moved the remaining Israel dossier page-local core stats, documented incidents, and money-trail graph into the canonical data module, added verifier guards against reintroducing page-local arrays/types, and live-verified production commit `b3bd5c9` on April 22, 2026.
- [x] P2 — Moved the Israel dossier expanded timeline, expanded stats, lobbying records, legal cases, and expanded incident source records into canonical structured exports, converted `israelDossierExpanded.ts` into a compatibility module, restored source-link audit coverage for the canon file, repaired stale Al Jazeera media URLs, and live-verified production commit `692728b` on April 22, 2026.
- [x] P2 — Added `npm run verify:israel-dossier:behavior`, a Playwright behavior verifier covering source-workbench filtering, money-trail expansion, carousel JPG generation, PDF export, iPhone SE/desktop overflow, and Chapter 15 public-preview evidence framing. Live-verified production commit `c873057` on April 22, 2026.
- [x] P2 — Wired Israel dossier verification into GitHub Actions: `verify:israel-dossier`, `verify:sources`, `npm run build`, `verify:bundle`, and `verify:israel-dossier:behavior` now run in `.github/workflows/verify-israel-dossier.yml`, with Playwright pinned in project devDependencies. The first workflow attempt exposed a CI-only WHO EMRO timeout; the workflow was stabilized with lower source-link concurrency plus retries/longer timeouts, and the second run passed end-to-end as run `24768886858` on commit `2c26e0c`.
- [x] P2 — Added first-class crawler/social metadata and generated-PDF text assertions to `npm run verify:israel-dossier:behavior`: `/israel-dossier` and `/chapter/chapter-15` now verify OG/Twitter title, description, canonical URL, `article` type, preview-image dimensions/content type, PDF download size, and actual PDF evidence text. Stabilized source-link CI with archive-backed/restricted-host classification, split the workflow into independent runtime and source-link jobs, and verified GitHub Actions run `24770703183` plus live production commit `603aab0` on April 22, 2026.
- [x] P2 — Added source-link trend reporting for restricted domains, archive-backed recoveries, retry-heavy URLs, transient/retry-limited fetches, status changes, and run-over-run deltas. The first CI-backed trend artifact passed on GitHub Actions run `24771280086` at commit `98f320f`, with 448 unique URLs, 0 missing, 0 failed, 0 invalid, 0 transient blockers, 84 restricted/bot-blocked URLs, and 2 retry-heavy URLs tracked as operator visibility instead of false hard failures.
- [x] P2 — Built the next Israel dossier/course expansion locally: added a six-module Evidence Course Path to `/israel-dossier`, exported it into the generated dossier PDF, created six source-safe Institute course/guide routes, added sitemap and machine-readable index coverage, and extended canon/behavior verification so the course path, module interaction, PDF text, and Institute URLs are now guarded. Local verification passed with `npx tsc --noEmit`, `npm run build`, `npm run verify:bundle`, `npm run verify:israel-dossier`, `npm run verify:israel-dossier:behavior -- http://127.0.0.1:4382`, `npm run verify:sources`, and a primary viewport matrix for `/israel-dossier` plus representative Institute course/guide routes. Deployment verification is still pending until the next push advances production.
- [x] P2 — Live-verified production commit `49a19b627432` / deployment `93e4009b-800a-4735-b015-03d740de41ee`: `/api/build-info` reported 288 prerendered routes, all five GitHub Actions workflows passed, `npm run verify:israel-dossier:behavior -- https://veritasworldwide.com` passed, the primary live viewport matrix passed on `/israel-dossier` plus representative course/guide routes, all twelve new Israel dossier course/guide URLs returned canonical prerendered HTML with JSON-LD, and the sitemap includes every new course/guide route.
- [x] P2 — Built the editor-ready Israel dossier artifact layer locally: added six public templates under `/israel-dossier/templates/`, wired each template into the canonical course path, exposed active-module template downloads and the Evidence Workbooks grid on `/israel-dossier`, added template links to `llms.txt`, included artifact labels in the dossier PDF, and extended source-link verification to scan public CSV/Markdown/JSON templates. Local verification passed with `npx tsc --noEmit`, `npm run build`, `npm run verify:bundle`, `npm run verify:israel-dossier`, strict `npm run verify:sources`, `npm run verify:israel-dossier:behavior -- http://127.0.0.1:4382`, direct manifest/template probes, and the primary viewport matrix. Deployment verification is pending until the next push advances production.
- [x] P2 — Live-verified production commit `b205bd8f9214` / deployment `21a42519-8209-4356-a8a4-abaf6c34879e`: all five GitHub Actions workflows passed, `/api/build-info` reported 288 prerendered routes, `npm run verify:israel-dossier:behavior -- https://veritasworldwide.com` passed with the new humanitarian attribution CSV download assertion, every static template file and `/llms.txt` template-manifest link resolved in production, and the primary live viewport matrix passed on `/israel-dossier` plus representative course/guide routes.
- [x] P2 — Built and live-verified the next Israel dossier populated record layer: added six source-labeled workbooks under `/israel-dossier/workbooks/` for the source ledger, aid ledger, humanitarian attribution table, incident evidence matrix, legal-status brief, and publishable briefing draft; exposed the populated pack on `/israel-dossier`; added workbook-manifest coverage to `llms.txt`; and extended canon, behavior, source-link, and visual checks to guard the records. Local verification passed with `npx tsc --noEmit`, `npm run verify:israel-dossier`, strict `npm run verify:sources` across 455 unique URLs with 0 actionable failures, `npm run build`, `npm run verify:bundle`, `npm run verify:israel-dossier:behavior -- http://127.0.0.1:4382`, direct workbook probes, and the full primary viewport matrix. Production commit `ea4c1a0` / deployment `5d1991c6-2a23-4cff-af28-8dd85335484e` then passed all five GitHub Actions workflows, `npm run verify:israel-dossier:behavior -- https://veritasworldwide.com`, live manifest/file probes, and the full primary live viewport matrix for `/israel-dossier` plus representative Institute course/guide routes.
- [x] P2 — Built and live-verified the public Israel dossier briefing surface: added canonical briefing sections from the populated workbook rows, routed `/israel-dossier/briefing`, linked it from the dossier Evidence Workbooks panel, added prerender/sitemap/LLM index coverage, wired bot metadata, and extended canon plus behavior verification for the briefing route, source-row IDs, unsafe-wording warnings, legal-posture interaction, and crawler metadata. Local verification passed with `npx tsc --noEmit`, `npm run build`, `npm run verify:bundle`, `npm run verify:israel-dossier`, strict `npm run verify:sources`, `npm run verify:israel-dossier:behavior -- http://127.0.0.1:4382`, and the full primary viewport matrix. Production commit `c7ec930` / deployment `d2f7235f-89e6-472b-9385-d35e95c8b27d` passed all five GitHub Actions workflows, `npm run verify:israel-dossier:behavior -- https://veritasworldwide.com`, crawler route probes, sitemap/LLM/workbook-link probes, and the full primary live viewport matrix across `/israel-dossier`, `/israel-dossier/briefing`, and `/institute/courses/write-israel-dossier-briefings`.
- [P2] Continue expanding the Israel dossier briefing into a larger chapter-style surface: add paragraph-level row IDs, downloadable chapter draft, and deeper section-by-section public-record notes without escalating beyond source posture.

- [x] P1 — Replaced the active operating canon in `VERITAS-PERFECTION-ENGINE.md` with the superseding v15.0 master directive supplied on April 8, 2026. Publication canon versus rights-package canon is explicit again, trust-first growth is the governing standard, and the prior prompt-level Institute AI exception is no longer active canon.
- [x] P1 — Closed the route-canon drift around `/about` and `/content-packs`: replaced the fabricated `AboutPage.tsx` newsroom copy with a truthful publication overview, wired `/about` into `src/App.tsx`, added `/about` to prerender coverage, and added `/content-packs` as a compatibility alias to the canonical `/content-pack` route so trust links and verification memory no longer point at dead surfaces.
- [P2] Live-verify the restored `/about` trust surface and `/content-packs` compatibility alias after production advances, then verify the deployed publication shell on `/`, `/read`, `/news`, `/membership`, and `/about` at 390px and 430px.

- [x] P1 — Root-caused deployed source-taxonomy drift on `main`: production was still serving the retired four-tier source hierarchy on `/methodology`, the foreword methodology copy, the signed-in source browser labels, and generated chapter provenance counts. Synced those surfaces back to the canonical five-tier model in a clean worktree, rebuilt chapter exports/prerender output, and prepared the trust-layer patch for live deployment.
- [x] P1 — Root-caused the mobile homepage dead zone on `/`: critical front-page modules were wrapped in scroll-triggered entrance styles that defaulted them to `opacity: 0`, so narrow viewports rendered a large blank region between the lead package and the table of contents until client-side intersection observers fired. The animation layer now renders immediately at 767px and below, and for reduced-motion users.
- [x] P1 — Root-caused public analytics resets: `server.js` now persists the analytics snapshot to Postgres via `analytics_state` when `DATABASE_URL` is available, keeps disk as fallback, flushes pageview/event mutations promptly, and loads analytics state before `app.listen` so a deploy no longer reboots the public dashboard to zero by default.
- [x] P2 — Root-caused public analytics title drift locally: `server.js` now resolves route-specific titles for static, chapter, profile, topic, news, and institute paths instead of trusting generic shell titles, while `src/hooks/usePageView.ts` now waits for route SEO effects before sending SPA pageviews to GA4, HubSpot, and `/api/analytics/pageview`.
- [x] P0 — Added live release observability to `server.js`: the app now exposes `/api/build-info`, sends `X-Veritas-Version` / `X-Veritas-Commit` / `X-Veritas-Deployment` headers when available, and returns real `404` responses for missing hashed assets or missing text artifacts instead of silently falling through to `index.html`.
- [x] P0 — Production deployment visibility recovered on April 7, 2026: live `/api/build-info` now returns `200`, `X-Veritas-*` headers are present, and `https://veritasworldwide.com/api/search?q=federal+reserve` now exposes `chapterType` plus public `availableEvidenceTiers` again. The earlier stale-deploy blocker is closed.
- [x] P0 — Re-ran the live logout revocation check after deploy: the same bearer token now gets `401` from `/api/auth/me` and `/api/downloads/the-record.pdf` after logout.
- [x] P1 — Added repo-native auth regression coverage via `npm run verify:auth` for anonymous preview, register, login, full chapter unlock, source-only search, protected PDF download, logout, revoked-token denial, and post-logout chapter downgrade.
- [x] P2 — Restored `chapterType`, `availableEvidenceTiers`, and `sourceHierarchyCounts` to public chapter exports so signed-out search and other public reference surfaces can render real structural metadata without exposing protected source rows.
- [x] P2 — Restored canonical evidence taxonomy on public trust surfaces: `/deep-state` now uses `Verified`, chapter 20 preview/generated copy no longer says `Alleged`, and `/methodology` now has working in-page anchor navigation plus canonical disputed-language copy.
- [P2] Verify the `/chapter/:id` and `/read` auth upgrade/downgrade flows in a real browser session at 390px and 430px widths.
- [P2] Confirm the deployed signed-out search and preview chapter payloads now expose restored public metadata (`chapterType`, `availableEvidenceTiers`, `sourceHierarchyCounts`) after production catches up to the latest build.
- [x] P3 — Consolidated the publication shell in `src/App.tsx`: primary destinations now stand apart from methodology/source utilities, mobile navigation is grouped instead of flat, header/footer buttons are quieter, and the shell keeps the Bible route and current route surface intact.
- [P2] Verify the deployed publication shell on `/read`, `/news`, and `/membership` at 390px and 430px widths, focusing on drawer grouping, sticky primary navigation, CTA clarity, and cookie-banner interference.
- [x] P1 — Reframed `src/pages/MembershipPage.tsx` around trust-first support: removed fabricated scarcity (`14 spots remaining`), removed unsupported urgency about future price increases, replaced this file’s remaining hardcoded colors with semantic tokens, and tightened the page copy so it reads like a public-interest funding surface instead of a generic CRO funnel.
- [P1] Verify the deployed analytics durability fix on `main`: confirm `/api/analytics/snapshot` survives a Railway deploy, then trigger a fresh pageview/event and verify the public dashboard increments without regressing to all zeroes.
- [P1] Verify the deployed analytics title normalization on `main`: confirm `/api/analytics/snapshot` no longer reports `The Record | Veritas Worldwide` for `/news`, `/profiles`, `/topics/:slug`, `/chapter/foreword`, and institute routes after a fresh deploy.
- [P2] Verify the updated membership copy and CTAs live on `/membership` at 390px and 430px widths, focusing on tone, CTA clarity, sticky bar behavior, and whether the softer copy still makes pricing and entitlements immediately legible.
- [x] P3 — Launched `Veritas Institute` as a separate learning-product surface with a distinct shell, 100-topic search-intent catalog, course pages, guide pages, methodology route, Book of Knowledge page, PDF export, and sitemap/prerender coverage for the new route family.
- [x] P3 — Added a dedicated front-page Veritas Institute module on `/` so the new learning vertical is discoverable from the editorial homepage, not just from shell utilities and footer links.
- [x] P3 — Rebuilt the Veritas Institute information architecture: the catalog now emphasizes track clusters, course pages now ship prerequisites/proof/sprint structure, guide pages now ship stronger answer-first hierarchy, the methodology page now explains the guide/course/book triad, and the Book of Knowledge export now carries richer course detail.
- [x] P2 — Verified the homepage institute module locally at 390px and 430px after the mobile visibility fix; the module now renders in flow instead of collapsing into dead air on narrow viewports.
- [P2] Verify the deployed homepage institute module on `/` at 390px and 430px widths, focusing on scanability, CTA clarity, and separation from the publication reading flow.
- [P2] Tighten the mobile cookie consent layer on `/`, `/methodology`, `/sources`, and `/membership` so fresh visits do not cover the first-screen trust copy and primary CTAs.
- [P2] Verify `/institute`, `/institute/book`, and representative `/institute/courses/:slug` plus `/institute/guides/:slug` routes at 390px and 430px widths in a real browser session.
- [P2] Confirm the new machine-readable Institute assets `/llms.txt` and `/veritas-institute.md` are reachable in production once deploy visibility is restored.
- [P3] Pressure-test the Book of Knowledge PDF export on desktop and mobile browsers; if generation is unstable at scale, move the manual export to a build-time or server-side artifact.
- [P3] Validate the rebuilt Institute route family in Search Console or equivalent after production catches up, focusing on crawlability, rich-result parsing, and index coverage for course/guide routes.
- [x] P3 — Added `npm run verify:sources`, wrote the source-link health report into `.claude-state/`, removed the unsupported `expandedArticlesB` news pack from the live corpus, and repaired deterministic chapter citation truncations. Actionable source issues dropped from 64 to 39.
- [x] P3 — Hardened the repo-native verification tools: `npm run verify:auth -- <base-url>` now honors the supplied target host, and `npm run verify:sources` now retries GET after bad HEAD responses instead of overstating broken citations.
- [x] P3 — Replaced a high-confidence batch of dead citation URLs with verified current or archive-backed links across `src/data/israelDossierExpanded.ts`, `src/data/profileData.ts`, `src/data/chapters/chapter-1.ts`, and `src/data/chapters/chapter-8.ts`. Actionable source issues dropped from 39 to 28.
- [x] P3 — Eliminated the remaining actionable source-link failures across `src/data/israelDossierExpanded.ts`, `src/data/profileData.ts`, and the residual chapter footnotes. `npm run verify:sources` now passes with 379 unique URLs checked and 0 actionable issues.
- [x] P3 — Operationalized the trust audit: existing source-link CI remains active, and the Israel dossier workflow now runs strict `npm run verify:sources` with CI-stabilized retries/timeouts, uploads the source-link report artifact, and passed on GitHub Actions with 448 unique URLs, 0 missing, 0 failed, and 0 invalid references.
- [x] P3 — Removed the remaining evidence-taxonomy drift risk from duplicated chapter source modules by turning `src/data/chapters.ts` into a module-backed bootstrap/search entrypoint and correcting the last legacy Chinese `Verified` label.
- [P4] Add production error monitoring (Sentry or equivalent) for auth, search, analytics, and prerender failures.
